name = "error-pages"
main = "worker.js"
compatibility_date = "2026-02-12"

# 路由配置 - 可选，部署后可以在 Cloudflare 控制台配置
# routes = [
#   { pattern = "your-domain.com/*", zone_name = "your-domain.com" }
# ]

# 环境变量 - 可选
# [vars]
# ENVIRONMENT = "production"

# 密钥配置 - 可选
# [secrets]
# API_KEY = ""
