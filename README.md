# HTTP 错误页面服务

一个支持动态生成的 HTTP 错误页面服务，可以部署到 Cloudflare Pages。

## 功能特性

- 🌍 自动检测浏览器语言（中英文）
- 🎨 Fluent Design 风格的精美错误页面
- 📱 完全响应式设计
- ⚡ 支持边缘计算部署

## 本地开发

### 方式一：Express 开发服务器
```bash
npm install
npm start
# 访问 http://localhost:3000/?code=404
```

### 方式二：Cloudflare Pages 本地预览
```bash
npm install
npm run pages:dev
# 访问 http://localhost:8788/?code=404
```

## 部署到 Cloudflare Pages

### 方式一：通过 Cloudflare Dashboard

1. 将代码推送到 Git 仓库
2. 登录 Cloudflare Dashboard
3. 进入 Pages -> 创建项目 -> 连接到 Git
4. 配置构建设置：
   - 构建命令：留空
   - 输出目录：`functions`

### 方式二：通过 Wrangler CLI

```bash
# 首次部署
npm run pages:deploy dist

# 或指定项目名称
npx wrangler pages deploy dist --project-name=your-error-pages
```

## 使用说明

部署后，可以通过以下方式访问：

- `/?code=404` - 显示 404 错误页面
- `/?code=500&lang=en` - 显示英文版 500 错误页面
- `/?code=403` - 显示 403 错误页面

支持的 URL 参数：
- `code`: HTTP 状态码（例如：404, 500, 403）
- `lang`: 语言（`zh` 或 `en`，可选，默认自动检测）

## 支持的状态码

### 4xx 客户端错误
400, 401, 402, 403, 404, 405, 406, 407, 408, 409, 410, 411, 412, 413, 414, 415, 416, 417, 418, 421, 422, 423, 424, 425, 426, 428, 429, 431, 451

### 5xx 服务器错误
500, 501, 502, 503, 504, 505, 506, 507, 508, 510, 511

### 3xx 重定向
300, 301, 302, 303, 304, 305, 306, 307, 308

### 2xx 成功
200, 201, 202, 203, 204, 205, 206, 207, 208, 226

### 1xx 信息响应
100, 101, 102, 103

## 项目结构

```
.
├── functions/
│   └── _middleware.js    # Cloudflare Pages 函数入口
├── public/
│   └── _headers           # HTTP 头部配置
├── server.js              # Express 开发服务器
├── package.json           # 项目配置
└── README.md              # 说明文档
```

## License

Copyright cay宸宇
